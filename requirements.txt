# ---- Core ----
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.2

# ---- Backend API ----
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9       # file uploads arrive as multipart/form-data

# ---- Database ----
# One set of models, two dialects: SQLite locally and in tests, Postgres in production.
sqlalchemy>=2.0.36
psycopg[binary]>=3.2          # Postgres driver, used only when DATABASE_URL is postgres

# ---- Authentication ----
argon2-cffi>=23.1             # password hashing; argon2id, no 72-byte truncation like bcrypt
python-jose[cryptography]>=3.3

# ---- LLM (OpenAI wire protocol drives all five providers) ----
langchain-core>=0.3.25
langchain-openai>=0.2.14

# ---- Document intelligence ----
pypdf>=5.1                    # PDF text + page numbers for citations
python-docx>=1.1              # DOCX
rank-bm25>=0.2.2              # keyword half of hybrid retrieval; pure Python, no torch
numpy>=1.26                   # cosine similarity over stored embedding vectors

# ---- Testing ----
pytest>=8.0
httpx>=0.27                   # FastAPI TestClient transport
